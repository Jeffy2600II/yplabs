'use client';

import React from 'react';

export default function LoaderProvider({ children }: { children: React.ReactNode }) {
  // Minimal loader provider stub — สามารถใส่ context / spinner logic ได้ที่นี่
  return <>{children}</>;
}