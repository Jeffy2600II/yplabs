'use client';

import React from 'react';

export default function PopupProvider({ children }: { children: React.ReactNode }) {
  // Minimal provider stub — ใส่ logic ของ popup/notifications ที่แท้จริงได้ที่นี่
  return <>{children}</>;
}